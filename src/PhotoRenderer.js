/**
 * This web component is basically usefull for the search query on the basis of the parameter 
 * it search for the photos and renders the photo, so one basic component is made to place the render logic
 * fetching the data from unsplash server using the unsplash-api
 *
 */
import React from 'react';
import unsplash from './Api';

class PhotoRenderer extends React.Component {
    render() {
        return (
            <div className="">
                
            </div>
        )
    }
}

export default PhotoRenderer;